# vimrc読書会 Nostrクライアント 実装計画

## 現在の状態

- フレームワーク: TanStack Start (React) セットアップ済み
- スタイリング: Tailwind CSS v4 導入済み
- テスト: Vitest + Testing Library 導入済み
- リンター: Biome 導入済み
- 既存コード: デモ用のHeader/Footer/ThemeToggle + ルーティング雛形のみ

---

## Phase 0: 設計決定（実装前に確定が必要）

- [x] Nostrライブラリ選定 → nostr-tools（ADR-001）
- [x] リレーサーバー構成の決定 → 環境変数管理、全リレー同時書き込み（ADR-004）
- [x] 状態管理手法の選定 → Zustand（ADR-002）
- [x] 認証・鍵管理方式の決定 → NIP-07 / 鍵生成 / nsec入力の3方式（ADR-003）
- [x] ディレクトリ構成の確定
- [x] UI/UX方針の確定 → シングルページ、デスクトップ優先

---

## Phase 1: 基盤（MUST — 他の全機能が依存する）

### 1.1 Nostrプロトコル基盤

> 前提: Phase 0 の Nostrライブラリ選定 / リレー構成が完了していること

- [x] Nostrライブラリのインストールと型定義の確認
- [x] リレー接続ユーティリティの実装
  - [x] WebSocket接続の確立
  - [x] 複数リレーへの並列接続
  - [x] 切断検知と指数バックオフによる自動再接続
  - [x] 接続状態の管理（connected / connecting / disconnected / error）
- [x] リレー設定の管理
  - [x] 環境変数 or 設定ファイルからリレーURLを読み込む
  - [x] リレーURL一覧の変更が可能な仕組み
- [x] イベント基本操作
  - [x] イベントオブジェクトの生成ユーティリティ
  - [x] イベント署名
  - [x] イベント検証（署名チェック）
  - [x] リレーへのイベント発行（publish）
  - [x] フィルタ指定によるイベント購読（subscribe / REQ）

### 1.2 認証・鍵管理

> 前提: Phase 0 の認証方式が決定していること

- [x] 鍵ペア生成（ランダム生成）
  - [x] 秘密鍵・公開鍵ペアの生成
  - [x] npub / nsec のbech32エンコーディング（NIP-19）
- [x] 鍵の保存と読み込み
  - [x] localStorageへの保存（暗号化検討）
  - [x] セッション復元（ページリロード時）
- [x] NIP-07対応（ブラウザ拡張連携）
  - [x] `window.nostr` の検出
  - [x] 拡張経由での署名リクエスト
  - [x] 拡張経由での公開鍵取得
- [x] 認証状態の管理
  - [x] ログイン / 未ログインの状態管理
  - [x] ログアウト処理

### 1.3 NIP-28 Public Chatチャンネル管理

NIP-28のkind一覧:
- `kind:40` チャンネル作成（メタデータ: name, about, picture）
- `kind:41` チャンネルメタデータ更新
- `kind:42` チャンネルメッセージ投稿
- `kind:43` チャンネルメッセージの非表示（モデレーション）
- `kind:44` チャンネルユーザーのミュート（モデレーション）

- [x] vimrc読書会用の固定チャンネル定義
  - [x] kind:40 でチャンネルを作成 or 既存チャンネルIDを設定ファイルで管理
  - [x] チャンネルメタデータ（name, about, picture）の取得と表示
- [x] チャンネルメッセージのフィルタ生成
  - [x] kind:42 イベントのフィルタ（チャンネル作成イベントIDでのeタグ絞り込み）
  - [x] eタグの先頭要素はチャンネル作成イベントID（NIP-28仕様）

---

## Phase 2: コア機能（MUST — チャットとして最低限動く）

### 2.1 メッセージ投稿（NIP-28 kind:42）

- [x] kind:42 チャンネルメッセージイベントの作成
  - [x] content にメッセージ本文を設定
  - [x] tags の先頭 e タグにチャンネル作成イベントID + 推奨リレーURL + "root" マーカーを設定
  - [x] created_at に現在時刻を設定
- [x] イベントの署名とリレーへの送信
- [x] 投稿中・投稿完了・投稿失敗のUI状態管理
- [x] 投稿フォームUI
  - [x] テキスト入力エリア
  - [x] 投稿ボタン
  - [x] 未ログイン時は投稿不可（閲覧のみ）

### 2.2 メッセージ表示とリアルタイム更新

- [x] チャンネルメッセージの取得（過去メッセージ）
  - [x] 初回ロード時のフィルタ（直近N件 or 期間指定）
  - [x] ページネーション or 無限スクロール
- [x] リアルタイム購読
  - [x] REQ サブスクリプションの管理
  - [x] 新着メッセージのリアルタイム表示
  - [x] EOSE（End of Stored Events）の処理
- [x] メッセージ一覧UI
  - [x] メッセージの時系列表示
  - [x] 自動スクロール（新着メッセージ到着時）
  - [x] スクロール位置の保持（過去ログ閲覧中に新着が来ても飛ばない）

### 2.3 ユーザーの区別

- [x] kind:0 メタデータの取得とキャッシュ
  - [x] 投稿者の公開鍵からプロフィール情報を取得
  - [x] メタデータのインメモリキャッシュ
  - [x] キャッシュの有効期限管理
- [x] メッセージへのユーザー情報表示
  - [x] 表示名（name フィールド）
  - [x] npub の短縮表示（フォールバック）

### 2.4 マークアップ（Markdown表示）

- [x] Markdownパーサーの導入
  - [x] ライブラリ選定（remark / marked / markdown-it 等）
  - [x] XSS対策（サニタイズ）
- [x] レンダリング
  - [x] 基本的なMarkdown記法（見出し、リスト、太字、斜体、リンク）
  - [x] コードブロックのシンタックスハイライト（Vimユーザー向けに重要）
  - [x] インラインコードの表示

### 2.5 Permalink

- [x] nevent / note ID の生成（NIP-19）
  - [x] メッセージイベントからneventを生成
  - [x] bech32エンコーディング
- [x] Permalink URL の構成
  - [x] URLパスまたはクエリパラメータでのイベント指定
- [x] Permalink先への遷移
  - [x] 該当メッセージへのスクロール
  - [x] 該当メッセージのハイライト表示

### 2.6 ログイン不要閲覧

- [x] 未認証状態でのリレー接続・メッセージ取得
  - [x] 認証なしでの REQ 送信
  - [x] 閲覧専用UIの表示
- [x] 認証状態に応じたUIの出し分け
  - [x] 未ログイン: メッセージ閲覧 + ログインボタン
  - [x] ログイン済み: メッセージ閲覧 + 投稿フォーム

### 2.7 アカウント作成

- [x] 鍵ペア生成フロー（Phase 1.2 の鍵生成を使用）
  - [x] 生成された鍵の表示（nsec のバックアップ警告付き）
  - [x] 鍵の保存
- [x] NIP-07連携フロー
  - [x] ブラウザ拡張の検出
  - [x] 拡張からの公開鍵取得
- [x] ログイン画面UI
  - [x] 鍵ペア生成ボタン
  - [x] NIP-07ログインボタン
  - [x] nsec直接入力フィールド（オプション）

### 2.8 プロフィール設定（名前）

- [x] kind:0 メタデータイベントの作成
  - [x] name フィールドの設定
  - [x] 既存メタデータの取得と上書き
- [x] プロフィール設定UI
  - [x] 名前入力フィールド
  - [x] 保存ボタン
  - [x] 設定反映の確認表示

### 2.9 bot対応

- [x] bot からの投稿が表示できることの確認
  - [x] 通常のkind:42イベントとして処理される（特別な対応不要）
- [x] bot 用の鍵ペア管理ドキュメント
  - [x] bot が使うnsecの管理方法の文書化

---

## Phase 3: 推奨機能（SHOULD）

### 3.1 メッセージの編集

- [x] 編集方式の決定（NIP-09ベースの削除+再投稿 or カスタム方式）
- [x] 編集イベントの作成・送信
- [x] 編集済みメッセージのUI表示（編集マーク）
- [x] 編集権限の確認（自分の投稿のみ編集可能）

### 3.2 アカウントのアイコン設定

- [x] kind:0 メタデータの picture フィールド対応
  - [x] アイコンURL入力
  - [x] アイコン画像の表示
- [x] メッセージ一覧でのアイコン表示
- [x] デフォルトアイコン（identicon等）

---

## Phase 4: 追加機能（MAY）

### 4.1 メッセージの削除

- [x] kind:5 削除イベントの作成（NIP-09）
- [x] 削除済みメッセージの表示処理（「削除されました」表示 or 非表示）
- [x] 削除権限の確認（自分の投稿のみ）

### 4.2 メッセージへのリアクション

- [x] kind:7 リアクションイベントの作成（NIP-25）
- [x] リアクション数の集計と表示
- [x] リアクションボタンUI

### 4.3 メッセージへの返信（NIP-28）

- [x] kind:42 での返信イベント作成
  - [x] 先頭 eタグ: チャンネル作成イベントID + "root" マーカー
  - [x] 追加 eタグ: 返信先メッセージイベントID + "reply" マーカー
  - [x] pタグ: 返信先メッセージの投稿者公開鍵
- [x] 返信先メッセージの表示（引用表示）
- [x] スレッド表示 or フラット表示の選択

### 4.4 メッセージへの引用

- [x] kind:42 + qタグでのイベント作成（NIP-18）
- [x] 引用先メッセージの展開表示

---

## Phase 5: E2Eテスト

- [x] Playwrightのセットアップ
- [x] モックリレーサーバーの構築
- [x] MUST要件のE2Eテスト
  - [ ] メッセージ投稿フロー（モックリレー統合済み、テスト追加可能）
  - [ ] リアルタイムメッセージ受信（同上）
  - [x] 未ログイン閲覧
  - [x] アカウント作成 → ログイン → 投稿
  - [x] プロフィール設定（ログインダイアログ内の名前設定 + 設定画面実装済み）
  - [ ] Markdown表示
  - [ ] Permalink遷移

---

## Phase 6: リリース準備

- [x] デモ用ファイルの削除（`src/routes/about.tsx` 削除済み）
- [x] 本番用リレーサーバーの設定（wss://yabu.me, wss://relay-jp.nostr.wirednet.jp）
- [x] README.md の更新（セットアップ手順、使い方）
- [x] ビルド確認（`pnpm run build` 成功）
- [x] デプロイ方法の決定と実施（Cloudflare Workers、wrangler deploy）

---

## 依存関係

```
Phase 0（設計決定）
  ↓
Phase 1（基盤: リレー接続、認証、チャンネル管理）
  ↓
Phase 2（コア: 投稿、表示、ユーザー区別、Markdown、Permalink、閲覧、アカウント、プロフィール）
  ↓
Phase 3（推奨: 編集、アイコン）  ←  Phase 5（E2Eテスト）は Phase 2 完了後から開始可能
  ↓
Phase 4（追加: 削除、リアクション、返信、引用）
  ↓
Phase 6（リリース準備）
```

## 備考

- TDD（Red→Green→Refactor）で実装する
- ユニットテストは実装と同時に書く。E2Eテストは Phase 5 で書く
- マイクロコミット（Conventional Commits）で進める
